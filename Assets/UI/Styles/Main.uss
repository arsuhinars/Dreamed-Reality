:root {
    color: rgb(255, 255, 255);
    font-size: 20px;
    -unity-font-style: normal;
    -unity-text-align: middle-left;
    -unity-font-definition: resource('Text/Fonts & Materials/Comfortaa-Regular SDF');
}
